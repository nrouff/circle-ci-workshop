  test:
  package:

  build:
    docker:
            - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run:
          name: gradle-test
          command:
            gradle test
      - run:
          command:
            gradle jar
      - store_test_results:
              path: build/test-results
      - store_artifacts:
              path: build/libs
              destination: prefix

